FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

# Point Vite's API proxy at the api container (not localhost)
ENV VITE_API_HOST=http://api:8000

EXPOSE 5173

CMD ["npx", "vite", "--host", "0.0.0.0"]
