services:

  # ── Trading agent scheduler ─────────────────────────────────────────────
  agent:
    build:
      context: .
      dockerfile: Dockerfile.local
    env_file: .env
    environment:
      THETADATA_HOST: host.docker.internal   # reach the shared ThetaData terminal on the host
      THETADATA_PORT: "25503"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: python main.py --paper --session paper-local
    volumes:
      - ./state:/app/state
      - ./backtest/sessions:/app/backtest/sessions
      - ./backtest/fixtures:/app/backtest/fixtures
      - ./.cache:/app/.cache
      - ./playbook:/app/playbook
    restart: unless-stopped
    depends_on:
      - api
    profiles:
      - trading   # only starts with: docker compose --profile trading up

  # ── FastAPI backend ──────────────────────────────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile.local
    env_file: .env
    environment:
      THETADATA_HOST: host.docker.internal   # reach the shared ThetaData terminal on the host
      THETADATA_PORT: "25503"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: uvicorn api.server:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - ./state:/app/state
      - ./backtest/sessions:/app/backtest/sessions
      - ./backtest/fixtures:/app/backtest/fixtures
      - ./.cache:/app/.cache
      - ./playbook:/app/playbook
      - ./api:/app/api
      - ./agents:/app/agents
      - ./tools:/app/tools
      - ./store:/app/store
    restart: unless-stopped

  # ── React frontend ───────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "5173:5173"
    depends_on:
      - api
    restart: unless-stopped
